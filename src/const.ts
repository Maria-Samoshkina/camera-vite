export enum AppRoute {
  Main = '/',
  NotFound = '*',
  Camera = '/camera'
}

export enum NameSpace {
Cameras = 'CAMERAS',
DetailedCamera = 'DETAILED_CAMERA'
}

export enum ApiRoute {
  Cameras = '/cameras',
}
